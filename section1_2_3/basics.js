// - Loops → While, for, Switch
// - Temporização (setInterval(function, 300)); setTimeout();
// - WebStorage → cookies(alguns valores que podem ser salvos no computador do usuario); Session(temporarias, quase mesma coisa que os cookies, mas quando o navegador fecha os cookies sao excluidos. Web storage → local ou session storage.
// - localStorage.setItem("nome”, "ogata”)
// - localStorage.getItem("ogata”)
// - localStorage.removeItem("nome") // chave
// - localStorage.nome = "Felipe”
// - Diferenças entre var, let e const
// - uso de variavel em string
// - Usar desconstrucao em objetos e arrays
// - Spread operator
// - Rest operator
// - Operacoes com array
// - Funcoes anonimas
// - Includes, endsWith e startsWith
// - Some and every
